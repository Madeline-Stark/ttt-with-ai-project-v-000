#!/usr/bin/env ruby

require_relative '../config/environment'

game = CLI.new
if game.over?
  puts "Would you like to play again? Y/n"
  answer = gets.strip
end

if answer == "Y"
  game = CLI.new
else
  puts "Later!"
  exit
end
