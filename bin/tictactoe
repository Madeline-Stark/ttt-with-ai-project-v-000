#!/usr/bin/env ruby

require_relative '../config/environment'

ceeli = CLI.new

#binding.pry
  if ceeli.game.over?
    puts "Would you like to play again? Y/n"
    answer = gets.strip
  end

  if answer == "Y"
    ceeli = CLI.new
  else
    puts "Later!"
    exit
  end
